name: Hello

on: 
    push:
        branches: [main]

jobs:
    greet:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v3

            - name: hello print
              run: echo "Hello from Github Actions!" 